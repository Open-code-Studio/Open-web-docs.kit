# 2026-07-01

## 新增首页支持
- 将根目录 `index.html` 从简单跳转页重写为完整的 MD3 风格首页
- 新增 `page/css/home.css`（350+ 行），包含 Hero、Features、Nav Cards、Links、Footer 等区域样式
- 扩展 `page/config.json`，新增 `home` 配置节点（heroTitle、heroSubtitle、features、primaryAction 等），完全向后兼容原有配置
- 首页功能：主题切换（dark/light）、滚动指示器、动态特性卡片、导航卡片、外部链接区
- 首页主题使用独立 localStorage key (`owd-home-theme`)，与文档页互不干扰
